#! /bin/bash

VERSION=0.3.7.2
ARCH=Linux32
if test $(uname -m) = x86_64 ; then
	ARCH=Linux64
fi

ARCHIVE=Popcorn-Time-${VERSION}-${ARCH}.tar.xz

cp -v /var/lib/fai/config/downloads/$ARCHIVE /target/tmp

$ROOTCMD tar --overwrite -xvJf /tmp/$ARCHIVE -C /usr/local/bin

