#! /bin/bash

error=0; trap 'error=$(($?>$error?$?:$error))' ERR # save maximum error code

fcopy -M /etc/apt/sources.list
$ROOTCMD sed -i -e "s/IPADDR/${IPADDR}/g" /etc/apt/sources.list

exit $error
